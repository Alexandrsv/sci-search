# This file will be committed to version control, so make sure not to have any
# secrets in it. If you are cloning this repo, create a copy of this file named
# ".env" and populate it with your secrets.

# When adding additional environment variables, the schema in "/src/env.js"
# should be updated accordingly.

# Application Configuration
APP_NAME="sci-search"
DOMAIN="${APP_NAME}.example.com" # Example domain for the application

# PostgreSQL Database Configuration
# These variables are used to construct DATABASE_URL and DATABASE_URL_DOCKER
POSTGRES_CONTAINER_NAME="${APP_NAME}-pg"
POSTGRES_DB="${APP_NAME}"
POSTGRES_USER="postgres_user"
POSTGRES_PASSWORD="your_db_password"
POSTGRES_PORT=5432 # Port where PostgreSQL is exposed on the host
POSTGRES_PORT_DOCKER=5432 # Internal Docker port for PostgreSQL

# Application Server Port
APP_PORT=3000

# Database Connection URLs
# DATABASE_URL is for connecting to a PostgreSQL instance on the host machine or a remote server
DATABASE_URL="postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:${POSTGRES_PORT}/${POSTGRES_DB}?schema=public"
# DATABASE_URL_DOCKER is for connecting to a PostgreSQL instance within a Docker network (e.g., in docker-compose)
DATABASE_URL_DOCKER="postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_CONTAINER_NAME}:${POSTGRES_PORT_DOCKER}/${POSTGRES_DB}?schema=public"

# Frontend Configuration (Publicly accessible variables for Next.js)
# Ensure these are prefixed with NEXT_PUBLIC_ if they are to be exposed to the client-side
NEXT_PUBLIC_YANDEX_METRIKA_ID="YOUR_YANDEX_METRIKA_ID"